# Full training dependencies — run locally with a CUDA GPU
# These are NOT installed on Streamlit Cloud (too heavy for free tier)
# The deployed app.py uses HuggingFace Inference API instead
torch==2.4.0
transformers==4.45.0
peft==0.13.0
trl==0.11.0
accelerate==1.0.0
bitsandbytes==0.44.0
datasets==3.0.0
jinja2==3.1.4
